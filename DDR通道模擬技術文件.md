# DDR 通道模擬：白話入門到進階

撰寫日期：2026-08-19
適用對象：剛開始做 DDR 訊號完整性模擬的工程師
文件性質：通用技術文件，不綁定特定案件或工具

---

## 先看這一頁：你真正要做的事

如果只想先知道順序，照下面做：

1. 先判斷要分析的是資料群，還是位址命令群。
2. 資料群一次分析一個 byte lane。
3. 資料群用 DQS 當時間基準，位址命令群用 CK 當時間基準。
4. 保留真實的參考平面、接地過孔、VDDQ 與終端。
5. 指定兩端真正的 IBIS 模型，不要讓工具猜驅動器與 ODT。
6. 先檢查 S 參數的埠數、被動性與因果性，再接電路模擬。
7. 寫入與讀取分開分析，因為它們是兩張不同的眼圖。
8. DDR4 之後，資料群看 Rx Mask 與 BER；位址命令群仍算 setup／hold。
9. 最差碼型用來看串擾，隨機碼型用來量時序。
10. typ、min、max 三個 Corner 都跑，最後由結果決定最差者。

**最重要的一句話：波形畫得出來，不代表模擬做對了；要確認訊號、時間基準與規格都對。**

---

## 目錄

| 章 | 主題 | 看完你會知道 |
|---|---|---|
| 一 | DDR 跟 SerDes 的差別 | 為什麼 PCIe 的做法不能直接搬到 DDR |
| 二 | DDR 匯流排的結構 | DQ、DQS、DM、ADCM 各自負責什麼 |
| 三 | 電氣設定 | VDDQ、Vref、ODT、POD 與驅動強度怎麼影響波形 |
| 四 | 時序與裕度 | 量到的時間為什麼不等於裕度 |
| 五 | 位址命令群 | 1T、2T、½T 為什麼會改變量測方式 |
| 六 | 完整模擬流程 | 從 Layout 一路走到眼圖與 Pass／Fail |
| 七 | 真正會改變答案的設定 | 哪些設定不能交給工具猜 |
| 八 | 進階問題 | 串擾、SSN、PDN 與 ODT 非線性怎麼影響結果 |
| 九 | DDR5 與 IBIS-AMI | DDR5 為什麼需要新的模型思維 |
| 十 | Corner 與統計 | 要跑哪些情況，最差結果怎麼找 |
| 十一 | 自我檢查 | 怎麼知道自己是不是算錯了 |

---

# 一、DDR 跟 SerDes 到底差在哪

**先記住：DDR 的資料是單端、雙向、成組同時切換；SerDes 通常是差動、單向、逐通道分析。**

做過 PCIe、USB 或 SATA 的人，接手 DDR 時最容易犯的錯，就是把 SerDes 的方法直接搬過來。

| | SerDes（PCIe、USB、SATA） | DDR |
|---|---|---|
| 訊號型態 | 差動 | **資料是單端**，只有選通與時脈是差動 |
| 時脈來源 | 從資料裡回復，叫 CDR | **另外送一條選通，叫 source-synchronous** |
| 方向 | Tx 與 Rx 各自單向 | **同一條線雙向**，要分讀取與寫入 |
| 通道數 | 一條一條分析 | **一整組同時切換**，例如 8 條 DQ 加 DQS 與 DM |
| 距離 | 通常較長，損耗主導 | 通常只有幾 cm，**反射與串擾主導** |

## 1.1 單端訊號需要可靠的參考

差動訊號像兩個人互相比較，外界一起加上的雜訊有機會被抵消。

單端訊號只有一個人說話，必須拿自己和參考電位比較。因此參考平面、回流路徑與電源雜訊，都會直接進入 DDR 的判讀。

**SerDes 可以忽略的細節，DDR 不一定能忽略。**

## 1.2 DQS 是資料的時間尺

DDR 不只看資料什麼時候翻轉，而是看資料相對於 DQS 什麼時候穩定。

可以把 DQS 想成裁判的哨聲，資料是選手動作。裁判在哨聲附近檢查資料，而不是只看資料自己什麼時候動。

所以 DDR 的時序是「相對時間」，不是單獨量一條線的絕對延遲。

**沒有正確的選通，就沒有正確的 setup／hold 裕度。**

## 1.3 讀取與寫入是兩套模型

寫入時，控制器是驅動端，記憶體顆粒是接收端；讀取時角色反過來。

驅動器、終端設定，甚至緩衝器型號都可能不同。因此寫入通過不代表讀取也通過，兩個方向要分開建立與分析。

## 1.4 一整組線會一起切換

一個 byte lane 通常有 8 條 DQ。當其中一條是受害線，其他 7 條都可能是攻擊線。

最差情況不是只有一條鄰線切換，而是其餘 7 條同時往反方向切換。資料運作久了，這種情況必然會出現，不能用「機率很低」忽略。

## 1.5 DDR 的主要敵人是反射與串擾

DDR 線路通常只有幾 cm，主要問題不是長距離損耗，而是阻抗不連續讓能量來回反彈。

因此 DDR 很重視終端匹配與驅動器輸出阻抗；SerDes 則比較常用 EQ，也就是等化器，處理長通道損耗。

---

# 二、匯流排的結構

**先記住：資料群與位址命令群不是同一種題目，因為它們的時間基準不同。**

## 2.1 資料群：一次看一個 byte lane

一個資料群通常以 byte lane，也就是位元組通道，為單位：

~~~
DQ0 ~ DQ7      8 條資料線，單端
DQS0_P/N       1 對選通，差動，是這一組的時間基準
DM0            1 條資料遮罩，單向
~~~

x16 顆粒有兩組 byte lane，也就是 Byte 0 與 Byte 1，每組都有自己的 DQS。

**實務上一次分析一組。** 不同 byte lane 沒有共同時間基準，混在同一次分析裡，工具不知道哪一組 DQS 才是原點。

## 2.2 位址命令群：用 CK 當時間基準

位址與命令群通常包含：

~~~
A0 ~ A15、BA0 ~ BA2（Bank Address）
RAS_n、CAS_n、WE_n、CS_n、CKE、ODT、RESET_n
CK_P/N
~~~

它有三個特徵：

- 單向，只有控制器驅動。
- 速率是資料的一半，屬於 SDR，只在時脈上升緣取樣。
- 可能扇出很大，一個控制器要推很多顆顆粒。

這不是資料群的縮小版。它的時間基準、UI、拓撲、埠數、規格參數與參考電壓都不同，後面第五章會另外說明。

## 2.3 電源與參考網路也要留下

VDDQ、VSSQ、VREF、VTT、ZQ 不是要觀察的資料訊號，但它們會影響回流路徑與終端。

如果裁切時只留下訊號線，卻切掉參考平面、接地過孔或電源回路，結果就不再代表原本的板子。

## 2.4 常見拓撲

| 拓撲 | 長相 | 初學者要注意什麼 |
|---|---|---|
| **點對點** | 控制器 ↔ 一顆顆粒 | 最單純，本文多數例子以此為基礎 |
| **Fly-by** | 控制器 → 顆粒 1 → 顆粒 2 → … | 各顆粒到達時間不同，要做 write leveling |
| **T 型分支** | 從主線中間分岔 | 分支點會反射，開路殘段是 SIwave 較不擅長的結構 |
| **DIMM** | 有連接器與模組板 | 連接器、多 Rank 與未選中顆粒的負載都要考慮 |

---

# 三、電氣：電壓、終端與驅動強度

**先記住：ODT 與驅動強度是設計設定，不是模擬工具量出來的答案。**

## 3.1 VDDQ 與 Vref

VDDQ 可以想成訊號使用的電源高度。Vref 像接收端的門檻線：高於它，接收端傾向判成 1；低於它，傾向判成 0。

| 世代 | VDDQ | Vref |
|---|---|---|
| DDR3 | 1.5 V | 0.5 × VDDQ |
| DDR3L | **1.35 V** | 0.5 × VDDQ |
| DDR4 | 1.2 V | **≈0.68 × VDDQ** |
| DDR5 | 1.1 V | 由訓練決定，per-DRAM 校準 |

DDR3 以前的 Vref 可以取 VDDQ 的一半，因為高低電位比較對稱。

DDR4 之後，I/O 改用 POD，也就是 Pseudo Open Drain，中文可理解成「偽開汲極」。終端接到 VDDQ，不是 VDDQ／2，所以高低準位不再對稱。

### 用數字看 DDR4 的 Vref

以下是 Micron 點對點設計指南 TN_4040 的例子：

~~~
VDDQ = 1.2 V
控制器 RON = 34 Ω
顆粒 ODT = 60 Ω

低準位 = 1.2 × 34 / (34 + 60) = 434 mV
理想 Vref = 434 + (1200 − 434) / 2
          = 816 mV
          = 0.68 × VDDQ
~~~

只要 ODT 或驅動強度改變，最佳 Vref 也可能改變。因此 DDR4 提供 VREFDQ 訓練，DDR5 更進一步做到 per-DRAM 校準。

## 3.2 ODT：把終端電阻放進晶片

ODT 是 On-Die Termination，也就是把終端電阻放在晶片裡，而不是全部放在板子上。

| ODT 值 | 好處 | 代價 |
|---|---|---|
| 低，例如 40 Ω | 比較能壓住反射 | 電流較大、低準位被拉高、眼高可能變小 |
| 高，例如 120 Ω | 眼高較大、省電 | 反射抑制較差，ISI 可能變大 |

常見方向是：寫入時顆粒開 ODT，例如 60 Ω；讀取時控制器開 ODT。沒有在通訊的那一端要關掉終端。

## 3.3 驅動強度就是輸出阻抗

驅動強度常用 RON 表示，可以把它想成訊號源本身帶著多少串聯電阻。

DDR4 常見 34 Ω 與 48 Ω，Micron 自家也會使用 40 Ω。

如果驅動器阻抗接近傳輸線阻抗，反射回來的能量比較容易被吸收。不匹配時，能量在線上來回彈，邊緣會變髒、眼睛會變小。

Micron TN_4040 第 27 頁的建議判定門檻是：

- Aperture DC ≥ 70％。
- 電壓裕度 ≥ 100 mV。
- 過衝 ≤ 200 mV。

## 3.4 模擬時不能讓工具猜

驅動強度與 ODT 通常寫在控制器與記憶體的暫存器設定裡。工具無法只看板子就知道你實際使用哪個值。

指定錯誤時，工具通常不會報錯，眼圖仍然畫得出來，只是那張圖代表一個實際系統不會使用的組態。

**「沒有錯誤訊息，但答案對錯對象」是 DDR 模擬最危險的靜默錯誤之一。**

---

# 四、時序：裕度怎麼算

**先記住：裕度不是量到的時間，而是「量到的時間減掉規格要求」。**

## 4.1 UI 是每個位元的時間尺

UI 是 Unit Interval，意思是一個位元所佔的時間：

~~~
UI = 1 / 資料率

DDR3L-1866 → 1 / 1866 MHz = 536 ps
DDR4-3200  → 1 / 3200 MHz = 312 ps
DDR5-6400  → 1 / 6400 MHz = 156 ps
~~~

任何單獨一項 setup 或 hold 如果超過一個 UI，先檢查設定與量測方法。因為一個位元的眼睛不可能比一個位元本身還寬。

## 4.2 Setup 與 Hold 是什麼

可以把 DQS 想成拍照快門。資料要在快門前先站穩一段時間，也要在快門後繼續保持一段時間。

~~~
        ←─ tDS ─→ ｜ ←─ tDH ─→
資料 ____╱‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾____
                  ↑
             選通邊緣，也就是取樣時刻
~~~

- tDS，Setup：資料在選通邊緣之前要穩定多久。
- tDH，Hold：資料在選通邊緣之後要維持多久。

這兩個是規格要求，不是工具量到的裕度。

## 4.3 用數字算一次裕度

~~~
量到的 Setup 時間    197.5 ps
規格要求 tDS        −140.0 ps
──────────────────────────
Setup 裕度             57.5 ps
~~~

量到的 197.5 ps 不是安全空間。扣掉規格要求 140.0 ps 後，真正的 Setup 裕度只有 57.5 ps。

## 4.4 AC 與 DC 是兩道不同的門

規格會給四個電壓門檻：

~~~
VIH(AC) / VIL(AC)    要衝過去的門檻，比較嚴
VIH(DC) / VIL(DC)    穩定維持的門檻，比較鬆
~~~

AC 像進門時要跨過的門檻，DC 像進門後只要留在屋內即可。

如果把兩組門檻填成一樣，Hold 往往會系統性偏小，因為工具會比較早判定訊號已經離開。

## 4.5 Derating：邊緣越慢，要求越嚴

JEDEC 的 tDS／tDH base 值假設訊號邊緣有一定速度。如果邊緣比較慢，訊號在門檻區停留較久，接收端比較不確定，所以要求要加嚴：

~~~
tDS(實際) = tDS(base) + ΔtDS（依你的 slew rate 查表）
~~~

這張表在 JEDEC 規格書裡，工具不會自動替你填好。

## 4.6 tVAC：真的跨過去並待住

tVAC 是 Valid AC time，可以理解成訊號在 AC 準位之外有效停留了多久。

訊號碰一下門檻又彈回來，和真正衝過門檻並站住，在某些簡單量法中可能相似；但接收端只有後者比較可能正確讀到。

## 4.7 DDR4 之後：資料群改看 Rx Mask 與 BER

DDR4 之後，資料群改用 Rx Mask，也就是眼圖中央不能被碰到的遮罩，並搭配 BER。

| | DDR3 | DDR4 以後 |
|---|---|---|
| 判準 | tDS／tDH 加電壓準位 | 眼圖要容納遮罩，BER < 10⁻¹⁶ |
| Slew rate derating | **需要** | **不需要**，遮罩已內含 |
| 抖動 | 另外處理 | 遮罩內含確定性與隨機抖動 |

DDR4 DQ 遮罩是眼寬 0.2 UI、眼高 130 mV。

優點是時間與電壓要求放進一個遮罩裡。代價是必須做出能外推到 BER 10⁻¹⁶ 的統計眼圖，暫態跑幾百個 UI 不夠。

---

# 五、位址命令群：另一種題型

**先記住：DDR4 的資料群看遮罩，DDR4 的位址命令群仍然看 tIS／tIH 加 derating。**

位址命令群又叫 ADCM 或 CA。它的時間基準、UI、拓撲、埠數、規格參數與參考電壓都和資料群不同。

| | 資料群 | 位址命令群 |
|---|---|---|
| 時間基準 | DQS 差動交越 | **CK 差動交越** |
| 速率 | DDR，兩緣取樣 | **SDR，只有上升緣取樣** |
| 一個位元佔多久 | 1 UI | **1 或 2 個時脈週期** |
| 方向 | 雙向，讀／寫 | **單向，只有控制器驅動** |
| 拓撲 | 點對點 | **Fly-by 一對多，加遠端終端** |
| 一條網路的埠數 | 2 | **顆粒數 ＋ 2** |
| 規格參數 | tDS／tDH | **tIS／tIH** |
| DDR4 判準 | 遮罩 ＋ BER | **setup／hold ＋ derating** |
| DDR4 I/O 型態 | POD | **SSTL 推挽** |
| DDR4 Vref | VREFDQ，內部訓練，≈0.68×VDDQ | **VREFCA，外部供給，0.5×VDD** |

同一片板子上，這兩群要用兩套判準與兩個 Vref。用一套設定跑完整片板子，至少有一群會被錯誤分析。

## 5.1 CK 是位址命令的時間基準

位址與命令是在 CK_t／CK_c 穿越零的時刻被取樣。

不要用單腳穿越 Vref 代替差動交越。串擾與 SSN 會讓共模電壓浮動，差動相減才比較能消除這個影響。

CK 是整條匯流排共用的時脈。它需要另外觀察：

- 週期抖動 tJIT(per)。
- 累積抖動 tJIT(cc)。
- 工作週期 tCH／tCL。

這些 CK 專屬項目不能直接套用到資料群。

## 5.2 1T、2T、½T 會改變搜尋視窗

Command Rate 決定一個位址在匯流排上維持幾個時脈週期：

~~~
1T   位址每個時脈週期換一次；CS_n 每個週期都可以下命令
     ┌──A──┬──B──┬──C──┐        位址視窗 = 1 × tCK
     └─CS──┴─CS──┴─CS─┘

2T   位址維持兩個時脈週期；CS_n 只在第二個週期拉低
     ┌─────A─────┬─────B─────┐  位址視窗 = 2 × tCK
     └──··──┴─CS─┴──··──┴─CS─┘

½T   CA 匯流排在時脈兩個邊緣都取樣，LPDDR4／5 的 CA 就是這樣
     位址視窗 = 0.5 × tCK
~~~

一個控制器要推 8 到 16 顆顆粒，負載重、扇出大，邊緣變慢，1T 的時間窗可能不夠。

2T 用多等一個時脈週期換來加倍的位址視窗，代價是命令晚一拍，效能掉幾個百分點。CS_n 不會跟著變成 2T，它仍然只拉低一個週期。

### 用 DDR3L-1866 算一次

~~~
tCK = 2 / 資料率

DDR3L-1866：資料率 1866 MT/s → tCK = 1.071 ns
資料群 UI  = 535.5 ps
位址 1T    = 1071   ps
位址 2T    = 2143   ps
~~~

如果拿 DQ 的 UI，也就是 535 ps，去量 2T 位址的 2143 ps，搜尋範圍只有實際的四分之一，很多線會回報沒有轉態。

反過來把 2T 當成 1T，視窗少一半，裕度會系統性偏小。

**開始分析前先確認 1T、2T 或 LPDDR 的 ½T。這是暫存器設定，不能從板子波形可靠推回來。**

## 5.3 Fly-by：一條線不只兩個埠

資料群常是點對點；位址命令群常是 Fly-by：

~~~
控制器 ──┬──────┬──────┬──────┬──── Rtt ── VTT
         │      │      │      │
        U1     U2     U3     U4
~~~

一條位址網路的埠數是顆粒數 ＋ 2。假設有 4 顆粒、16 條位址線，埠數就是 4 ＋ 2，再乘 16，也就是 96 個埠。

三個後果：

1. 每一顆都要量，最差的不一定是最遠那顆。
2. 每個分支都是開路殘段，可能產生 λ／4 共振。
3. VTT 終端是電路的一部分，不能當成未接埠或直接刪掉。

### Write leveling 是設計，不是錯誤

Fly-by 故意讓每顆粒收到 CK 的時間不同，以分散 SSN。DDR3 之後，控制器用 write leveling 為每顆粒個別延遲 DQS。

所以各顆粒 CK 到達時間差幾百 ps，不代表佈線一定錯。但位址相對 CK 的關係，在每顆位置都必須成立。

## 5.4 位址命令的規格參數與電壓

位址與命令用 tIS（input setup）與 tIH（input hold），相對 CK 量測，並要查 derating 表。CKE 有自己的參數，例如 tIHCKE。

DDR4 資料群用 Rx Mask 加 BER；位址命令群仍用 tIS／tIH 加 derating。

| | DDR4 資料群 | DDR4 位址命令群 |
|---|---|---|
| I/O 型態 | POD，偽開汲極 | **SSTL，推挽** |
| 終端接到 | VDDQ | **VTT = 0.5×VDD** |
| Vref | VREFDQ，內部訓練，典型 ≈0.68×VDDQ | **VREFCA，外部供給，0.5×VDD** |

拿 0.68×VDDQ 去判位址，可能不會報錯，但會讓 setup 系統性偏大、hold 系統性偏小。

## 5.5 位址群還要看邊緣是否單調

資料群常看眼寬與眼高。位址群還要看訊號從 VIL(AC) 到 VIH(AC) 的途中，有沒有停住或回頭。

~~~
  VIH(AC) ─────────────────╱───
                        ╱
  Vref    ────────────┤        ← 台階：訊號可能停住或回頭
                    ╱
  VIL(AC) ──────╱───────────────
~~~

JEDEC 要求這段訊號保持單調。不單調時，接收端可能在同一個邊緣判讀兩次，也就是 double clocking。

眼圖折疊後可能看不出這個台階，所以要用 single-shot 單一波形檢查。

## 5.6 位址命令群自檢表

- [ ] 時間基準是 CK 差動交越，不是 DQS，也不是單腳穿越 Vref。
- [ ] 已確認控制器是 1T、2T 或 LPDDR 的 ½T。
- [ ] 搜尋視窗使用位址自己的週期，不是資料群 UI。
- [ ] CS_n 用 1T 視窗判定，即使位址是 2T。
- [ ] 埠數 = 顆粒數 ＋ 2，並包含 VTT 終端。
- [ ] 每一顆都量過，不是只量最遠那顆。
- [ ] VTT 終端電阻在電路中，沒有被當成未接埠。
- [ ] 使用 tIS／tIH 加 derating，不是 tDS／tDH。
- [ ] DDR4 位址仍算裕度，沒有跟資料群一起改用遮罩。
- [ ] DDR4 位址使用 VREFCA = 0.5×VDD，不是 0.68×VDDQ。
- [ ] 另外用 single-shot 波形檢查邊緣單調性。
- [ ] 求解器可信頻寬有考慮分支殘段，而且比資料群更嚴格。

---

# 六、模擬流程的六個階段

**先記住：每一階段都要留下可以單獨檢查的產出，出錯時才知道是哪一段出錯。**

~~~
① Layout（.brd／.aedb）
      ↓ 選網路、裁切
② 通道幾何（只剩要分析的那一組）
      ↓ 建 Port
③ 有埠的模型
      ↓ 電磁場求解（SIwave 或 HFSS）
④ S 參數（Touchstone，N 埠）
      ↓ 接上 IBIS 緩衝器
⑤ 電路模擬（Transient 或統計）
      ↓ 以選通為基準量測
⑥ 眼圖 ＋ 逐訊號裕度 ＋ Pass／Fail
~~~

## 6.1 從 Layout 裁切通道

從整片板子切出只包含一組訊號的區域，目的是降低求解規模。

一定要保留訊號線、參考平面、鄰近接地過孔與元件接腳。

最常見的錯是外框貼太緊，把 BGA 逃線區周圍的接地球切掉。訊號線看似還在，但回流路徑已經不完整，Port 的參考端可能站不住。

## 6.2 建立 Port

在控制器與顆粒接腳兩端建立激勵埠。Port 的正端接訊號腳，負端接參考。

| Port 方式 | 負端接什麼 | 適用情況 |
|---|---|---|
| Coax，同軸 | 過孔周圍的參考平面 | 通孔結構 |
| Circuit，座標 | 參考平面上的某一點 | 一般 SMD |
| **Pin Group** | **元件所有參考腳合在一起** | **細間距 BGA** |

細間距 BGA 優先使用 Pin Group。訊號球正下方通常是 antipad，也就是平面挖空區；座標型負端放在那裡，可能沒有真正接到平面。

求解器遇到不完整 Port，有時會靜默丟掉，不一定報錯。Touchstone 可能因此少了幾個埠。

## 6.3 電磁場求解：SIwave 還是 HFSS

**不是模型越複雜就一定用 HFSS，而是看結構相對於最高頻率有多複雜。**

同一顆 BGA launch，在 20 GHz 可能需要 HFSS；在 3 GHz 可能只像一小段集總電感。

| 結構 | SIwave 的參考可信範圍 |
|---|---|
| 均勻帶狀線、平面走線 | 很高，這是 SIwave 的強項 |
| 走線彎折 | 通常不是問題，實測與 HFSS 差 0.3 dB |
| 跨越參考平面分割 | 約 13 GHz |
| **開路殘段／T 型分支** | **約 7 GHz**，λ／4 共振可能被放大 |

這些數字會受工具與版本影響，最好用自己的案例驗證一次。

DDR3L-1866 的邊緣拐點約 2～3.5 GHz，DDR4-3200 約 5 GHz，都在上述 SIwave 參考範圍內。因此多數 DDR 板級通道用 SIwave 就足夠，而且可能快一到兩個數量級。

掃頻至少到基頻的 5 倍，或用：

~~~
f_knee = 0.35 / Tr
~~~

DDR3L-1866 基頻是 933 MHz，掃到 5～10 GHz 通常已足夠。掃到 50 GHz 多半只是浪費時間。

## 6.4 接上 IBIS

S 參數只是通道，不是完整晶片行為。兩端要接緩衝器模型。

DDR 兩端可能是兩家不同料號：控制器一份 .ibs，顆粒一份 .ibs。只給一份模型，可能讓一側用錯驅動器，而且工具不一定報錯。

整組道要一起求解，不能把 8 條 DQ 拆成 8 次獨立分析，否則道間耦合與串擾會消失。

## 6.5 以選通為基準量測

最後以 DQS 差動交越點為時間原點，逐條資料訊號量 setup／hold。

不能用單腳穿越 Vref 代替 DQS 差動交越，因為 DQS 的共模會隨串擾浮動。

---

# 七、每一階段真正會改變答案的設定

**先記住：下面這些設定一旦錯了，眼圖可能仍然漂亮，但結論會是另一個系統的結論。**

## 7.1 參考網路不能只選 GND

DDR 的回流同時走地與 VDDQ。POD 終端接到 VDDQ，只給 GND 會少一半回流路徑。

## 7.2 DQS 與資料要先對齊

寫入時，控制器把 DQS 對齊在資料眼中央，相對資料位移約 0.5 UI。

讀取時，顆粒送出的 DQS 通常是邊緣對齊，控制器會自己延遲 90° 再取樣。

沒有這個位移，setup 與 hold 可能都趨近於零。這不一定是通道很差，也可能只是選通沒有對齊。

## 7.3 碼型要依目的選

| 碼型 | 用途 | 不能拿來做什麼 |
|---|---|---|
| **決定性最差碼型** | 看串擾，讓攻擊者對齊 | 不適合量 setup／hold |
| **PRBS／隨機碼型** | 量時序，需要頻繁轉態 | 不容易保證看到最差串擾 |

用最差碼型量時序，常會量不到約束，因為選通邊可能落在資料沒有轉態的地方。

用隨機碼型看串擾，幾乎確定看不到最差情況。N 條道同時反向切換的機率是：

~~~
(1 / 4)^(N − 1)
~~~

8 條道要跑兩萬個 UI，才有一次期望值。

所以串擾用決定性最差碼型，時序用 PRBS／隨機碼型。

## 7.4 未接埠要明確處理

沒有綁到緩衝器的埠，必須明確指定是終結、開路還是接地。

開路與終結會產生不同串擾結果，兩者都不是安全預設值。

## 7.5 讀取與寫入都要做

雙向匯流排的兩個方向是兩張不同眼圖，工具沒有足夠資訊自動代選。

DM 在顆粒端是單向訊號。讀取時顆粒沒有 DM 驅動器，這不是錯誤；讀取分析要把 DM 排除。

---

# 八、進階：串擾、SSN、PDN 與 ODT 非線性

**先記住：只模擬乾淨通道，通常會高估實機裕度；電源與多線同時切換會把眼睛再縮小。**

## 8.1 串擾：攻擊者不是一條，而是七條

一個 byte lane 中，受害線旁邊其他 7 條線都可能切換。

最差情況是其他 7 條同時往反方向切換。近端串擾叫 NEXT，通常在驅動端較大；遠端串擾叫 FEXT，會隨耦合長度累積。DDR 距離短、方向又可能雙向，所以兩者都要看。

## 8.2 SSN：大家一起切換，電源就會晃

SSN 是 Simultaneous Switching Noise，也就是同時切換雜訊。

多個 I/O 一起切換時，電源網路供應瞬間電流，封裝與 PCB 電感產生壓降，讓晶片裡的「地」跟著跳。

結果是高低準位偏移、眼高變小，以及切換時刻偏移、產生 PSIJ，也就是 Power Supply Induced Jitter。

DDR5 的實測數量級（Xpeedic 2022）：加入實際 PDN 後，眼寬減少 **13 ps**，眼高減少 **8 mV**。

DDR3／DDR4 的 SSN 深入分析（DesignCon，Yoon）：

| 條件 | 抖動 |
|---|---:|
| 理想，無 ISI、無串擾、無 SSN | 0.058 UI |
| ＋ISI | 0.078 UI |
| ＋ISI ＋串擾 | 0.183 UI |
| ＋ISI ＋SSN | 0.216 UI |
| **＋ISI ＋串擾 ＋SSN** | **0.31 UI** |

0.058 UI 到 0.31 UI，約放大 5 倍。**只看通道眼圖，會高估裕度。**

## 8.3 PDN 要不要一起模擬

理想做法是把 PDN 的 S 參數與通道一起接進電路模擬，讓緩衝器電源腳看到真實阻抗，而不是理想電壓源。

代價是模型變大、求解變慢。可以用 DesignCon 的 MCF 方法：

1. 跑一次理想電源暫態。
2. 跑一次接真實 PDN 的暫態。
3. 比較眼高與眼寬差，得到 MCF，也就是 Mask Correction Factor。
4. 把 MCF 加到遮罩，後面的統計分析使用修正後的遮罩。

實測案例中，DDR4 遮罩由眼寬 0.2 UI、眼高 130 mV 修正成眼寬 **0.23 UI**、眼高 **224 mV**，模擬與實測差異只有 **2％**。

## 8.4 ODT 可能不是固定電阻

| | 被動式，Poly 電阻 | 主動式，PMOS |
|---|---|---|
| 面積 | 大 | **小** |
| PVT 敏感度 | 低 | **高** |
| I-V 線性度 | 好 | **差** |

現代 DDR 多使用主動式 ODT，終端阻抗會隨輸出電壓改變，不是固定常數。

DesignCon，Yoon 的實測重點是：**校準點比線性度更重要。**

~~~
0.75×VCC：0.254 UI
0.35×VCC：0.31 UI
~~~

POD 拓撲下，校準在 0.75×VCC 的抖動比 0.35×VCC 更好，即使前者串擾更大。

IBIS 的 I-V 曲線是在某個工作點校準的。實際工作點不同，等效阻抗就不一定等於標稱值。

---

# 九、DDR5 與 IBIS-AMI

**先記住：DDR5 從 3200 提高到 6400 MT/s 後，普通 IBIS 可能不足以描述等化與單端取樣。**

## 9.1 為什麼需要 AMI

3200 → 6400 MT/s 後，通道損耗增加，可能需要 FIR、CTLE、DFE 等化。

- FIR：發送端預加重。
- CTLE：連續時間線性等化。
- DFE：決策回授等化。

這些是行為模型，普通 IBIS 不一定能完整描述。

## 9.2 普通 IBIS-AMI 的四個限制

Xpeedic 在 2022 Asian IBIS Summit 列出的限制（DDR5.pdf 第 4 頁）：

1. 不支援 PDN 效應。
2. 只支援差動訊號，但 DDR 的 DQ 是單端。
3. 只支援點對點，但 DDR 可能有多 Rank、多 DIMM。
4. 不支援外部時脈，但 DQ 是由 DQS 觸發的。

不能因為有 AMI，就把 DDR5 當成 PCIe。模型能力要和真實拓撲、單端特性與時脈來源對得上。

## 9.3 DDR5 的三個技術重點

### DER：上升與下降要分開看

DER，bit-by-bit，取代 SBR。

POD 的高低準位不對稱，上升與下降邊緣也不一定對稱。SerDes 可能一個步階響應就夠，但 DDR 要分別取得上升與下降的步階響應。

### DC_Offset：先拿掉直流偏移

單端訊號不是繞著零電壓上下擺動。統計眼圖要先拿掉直流偏移，再把它加回來：

~~~
V_dc_offset = (V_high + V_low) / 2
~~~

這個做法與 BIRD197.7 有關。

### Clock Forwarding：資料與時脈一起送進接收器

IBIS 7.1 開始，Rx 的 AMI_GetWave 可以同時接收資料與時脈波形。

因為 DDR 的取樣時刻來自 DQS，不是接收端從資料裡自己回復時脈。

---

# 十、Corner 與統計

**先記住：最差的不一定是慢速 Corner，typ、min、max 三個都跑完再排序。**

## 10.1 IBIS 的三組 Corner

IBIS 通常提供 typ、min、max：

~~~
min = Slow，慢邊緣、弱驅動、低電壓、高溫
max = Fast，快邊緣、強驅動、高電壓、低溫
~~~

## 10.2 Fast 也可能是最差

慢邊緣不一定最差。快邊緣的反射通常更強，因為上升時間短，阻抗不連續產生的反射能量更集中，串擾也增加；串擾通常與 dV／dt 有關。

實務上可能遇到 Fast 是最差 Corner。因此三個都跑，最後由眼高、眼寬、裕度與 BER 排序。

## 10.3 暫態與統計各自負責不同工作

| | 暫態 Transient | 統計 Statistical |
|---|---|---|
| 做法 | 計算幾百個 UI 的真實波形 | 由脈衝響應摺積出機率分布 |
| 能到的 BER | 約 10⁻³ | 可外推到 10⁻¹⁶ |
| 能不能看串擾最差 | **可以**，用決定性碼型 | 只能統計 |
| 非線性 | **保留** | 假設線性 |

DDR4 之後的遮罩判定要求 BER 10⁻¹⁶，所以需要統計方法；串擾最差對齊仍要靠暫態的決定性碼型。兩種分析都要做。

---

# 十一、怎麼知道自己算錯了

**先記住：看到一張眼圖，不代表模型正確；先照清單逐項檢查。**

## 11.1 幾何與埠

- [ ] 裁切後接地過孔還在嗎？BGA 周圍最容易被切掉。
- [ ] Touchstone 埠數正確嗎？22 個埠應該是 .s22p。
- [ ] 如果埠數不對，有沒有檢查求解器是否靜默丟掉埠？
- [ ] 每個埠都有有效參考嗎？沒有參考時，結果可能接近全反射。

## 11.2 S 參數本身

- [ ] 插入損耗在基頻是零點幾 dB 的量級嗎？如果是 −50 dB，可能量到串擾，不是直通路徑。
- [ ] 回波損耗是否有比 −10 dB 更糟的頻段？
- [ ] 被動性是否成立？|S| 有沒有大於 1？
- [ ] 因果性是否成立？階躍響應在 t=0 之前有沒有不應存在的訊號？

## 11.3 時序數字

- [ ] setup 或 hold 單獨一項有沒有超過一個 UI？
- [ ] setup ＋ hold 加起來合理嗎？應接近「UI 減去邊緣時間」。
- [ ] setup 與 hold 都趨近於零嗎？可能是 DQS 沒有相位對齊。
- [ ] 大部分通道都量不到約束嗎？可能是碼型選錯，用了最差碼型量時序。

## 11.4 模型綁定

- [ ] 兩端綁到的是不同料號嗎？DDR 兩端不一定是同型號。
- [ ] 驅動強度與 ODT 是你指定的值，還是工具猜的？
- [ ] 讀取方向有把 DM 排除嗎？

## 11.5 最後的結論

- [ ] 裕度是「量到的時間 − 規格要求」，不是量到的時間本身嗎？
- [ ] tDS／tDH 有含 derating 嗎？
- [ ] typ、min、max 三個 Corner 都跑了嗎？最差的是哪一個？
- [ ] 有沒有把 PRBS 結果誤標成最差情況？**不可以。**

---

# 附錄：延伸閱讀

以下列出 19 份 DDR4／DDR5 延伸教材中的建議閱讀順序。這裡先列出最適合建立主線觀念的 8 份：

| 順序 | 檔案 | 為什麼值得先看 |
|---|---|---|
| 1 | TN_4040_DDR4_Point_to_Point_Design_Guide.pdf | Micron 官方資料，包含 VREF、POD、驅動與 ODT 建議組合表 |
| 2 | 6_TH5Slides_DDR4BoardDesignAndSignal.pdf | DDR4 板級設計全貌 |
| 3 | Accurate_Statistical_Based_DDR4_Margin_Estimation… | DDR4 為何改用遮罩與 BER，以及 MCF 方法 |
| 4 | SLIDES_07_IndepthSSNAnalysisofDDR3DDR4_Yoon.pdf | ODT 非線性與 SSN 的實測 |
| 5 | DDR5.pdf | DDR5 的 AMI 需求、DER、DC_offset 與時脈前饋 |
| 6 | 6-TA2_Slides_ChanneltoChannelCrosstalkBehavior.pdf | 通道間串擾行為 |
| 7 | 11-TA2_Slides_InnovativePDNDesignGuidelines.pdf | PDN 設計 |
| 8 | 11_WE5Slides_Touchstonev2SIPISParameterModels.pdf | Touchstone v2 與 S 參數模型品質 |

其餘資料包含 LPDDR3、LPDDR4 專題與各家實作經驗，等做到那些世代時再閱讀即可。

---

## 一句話總結

**DDR 模擬最難的地方，不是把數字算出來，而是確認自己算的是正確的事情。**

方向對不對、時間基準是不是 DQS 或 CK、裕度有沒有減掉規格要求、最差碼型有沒有真的出現，任何一項弄錯，眼圖都可能畫得出來，而且看起來還很正常。
