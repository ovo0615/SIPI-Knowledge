# DDR 通道模擬：從基礎到進階

撰寫日期：2026-08-19
對象：要開始做 DDR 訊號完整性模擬，但不知道從哪裡問起的工程師
性質：**通用技術文件**，不綁任何特定案子或工具

---

## 目錄

| 章 | 內容 | 讀完你會 |
|---|---|---|
| 一 | DDR 跟 SerDes 到底差在哪 | 知道為什麼 SerDes 的做法搬過來會錯 |
| 二 | 匯流排的結構 | 看得懂 DQ／DQS／DM／ADCM 各是什麼 |
| 三 | 電氣：電壓、終端、驅動強度 | 知道 VDDQ／Vref／ODT／POD 在講什麼 |
| 四 | 時序：裕度是怎麼算出來的 | 分得清「量到的時間」與「裕度」 |
| 五 | 位址命令群 | 知道它為什麼是另一種題型，1T／2T 差在哪 |
| 六 | 模擬流程的六個階段 | 知道整條路長什麼樣 |
| 七 | 每一階段真正的決定點 | 知道哪些選項會改變答案 |
| 八 | 進階：串擾、SSN、PDN、ODT 非線性 | 知道為什麼眼圖在實機上比模擬差 |
| 九 | DDR5 與 IBIS-AMI | 知道下一代要多做什麼 |
| 十 | Corner 與統計 | 知道要跑幾次、哪一次最差 |
| 十一 | 怎麼知道自己算錯了 | 有一份可以逐條打勾的自檢表 |

---

# 一、DDR 跟 SerDes 到底差在哪

做過 PCIe／SerDes 的人接手 DDR，最容易犯的錯是**把 SerDes 的方法直接搬過來**。
兩者有五個根本差異：

| | SerDes（PCIe、USB、SATA） | DDR |
|---|---|---|
| 訊號型態 | 差動 | **資料是單端**，只有選通與時脈是差動 |
| 時脈來源 | 從資料裡回復（CDR） | **另外送一條選通（source-synchronous）** |
| 方向 | 各自單向，Tx 與 Rx 分開 | **同一條線雙向**（讀與寫） |
| 通道數 | 一條一條算 | **一整組同時切換**（8 條 DQ ＋ 選通 ＋ 遮罩） |
| 距離 | 長，損耗主導 | 短（幾 cm），**反射與串擾主導** |

這五點各自的後果：

**資料是單端** → 沒有差動的共模抑制，所以參考平面、回流路徑、電源雜訊會
直接進到訊號裡。SerDes 可以忽略的東西，DDR 不能。

**Source-synchronous** → 時序不是絕對的，是**相對於選通**的。板子造成的延遲
在 DQ 與 DQS 上會互相抵消，所以你量的是「差」，不是「絕對值」。
**沒有選通就沒有時序裕度這個量。**

**雙向** → 讀與寫是兩張完全不同的眼圖：驅動端不同、終端設定不同、
甚至用的緩衝器型號都不同。做完寫入不代表讀取也過。

**一整組同時切換** → 串擾不是「一條攻擊者」，是「七條同時往反方向切換」。
而且最惡劣的那個瞬間**必然會發生**，不是機率問題。

**短距離** → 主要敵人是阻抗不連續造成的反射與 ISI，不是損耗。
所以終端匹配（ODT）與驅動強度是 DDR 的核心設計參數，SerDes 則是 EQ。

---

# 二、匯流排的結構

## 2.1 訊號分成三群

一組 DDR 介面的訊號可以分成三類，**它們的模擬方式不同**：

### 資料群（Data Group）

以 **byte lane（位元組通道）** 為單位，一組包含：

```
DQ0 ~ DQ7      8 條資料線（單端）
DQS0_P/N       1 對選通（差動）        ← 這一組的時間基準
DM0            1 條資料遮罩（單向）
```

x16 的顆粒有兩組（Byte 0 與 Byte 1），各自有自己的選通。
**一次分析一組**——不同 byte lane 之間沒有共同的時間基準，混在一起沒有意義。

### 位址命令群（Address / Command，簡稱 ADCM 或 CA）

```
A0 ~ A15、BA0~2（Bank Address）、
RAS_n、CAS_n、WE_n、CS_n、CKE、ODT、RESET_n
CK_P/N                                  ← 這一群的時間基準是時脈
```

特性：**單向**（只有控制器驅動）、**速率是資料的一半**（SDR，只在時脈上升緣
取樣）、但**扇出可能很大**（多顆顆粒時是一對多）。

> **這一群是另一種題型，不是資料群的變體。** 時間基準、UI、拓撲、埠數、
> 規格參數、參考電壓六件事全部不一樣，整章講在**第五章**。

### 電源與參考

`VDDQ`、`VSSQ`、`VREF`、`VTT`、`ZQ`。這些不是訊號，但**回流路徑走它們**，
所以在裁切通道時必須包含進來當參考網路。

## 2.2 為什麼要分群

因為**時間基準不同**。資料群以 DQS 為基準、位址命令群以 CK 為基準。
把兩群放在同一次分析裡，工具不知道該用哪一個當原點。

實務上就是：一次做一個 byte lane，或一次做整個 ADCM 群。

## 2.3 拓撲

| 拓撲 | 長相 | 難點 |
|---|---|---|
| **點對點** | 控制器 ↔ 一顆顆粒 | 最單純，本文預設 |
| **Fly-by** | 控制器 → 顆粒1 → 顆粒2 →…（DDR3 之後的 ADCM 標準做法） | 各顆粒收到的時間不同，要做 write leveling |
| **T 型分支** | 從中間分岔 | 分支點是反射源；**開路殘段是 SIwave 最不擅長的結構** |
| **DIMM** | 有連接器與模組板 | 連接器要另外建模，多 Rank 要考慮未選中那個的負載 |

---

# 三、電氣：電壓、終端、驅動強度

## 3.1 電壓

| 世代 | VDDQ | Vref |
|---|---|---|
| DDR3 | 1.5 V | 0.5 × VDDQ |
| DDR3L | **1.35 V** | 0.5 × VDDQ |
| DDR4 | 1.2 V | **≈0.68 × VDDQ**（見下） |
| DDR5 | 1.1 V | 由訓練決定，per-DRAM 校準 |

**Vref 是接收端判 1／0 的分界線。** DDR3 以前它就是電壓擺幅的正中央，
所以直接除以二。

**DDR4 之後不能再除以二。** 原因是 I/O 從推挽式的 SSTL 改成
**POD（Pseudo Open Drain，偽開汲極）**：終端接到 **VDDQ** 而不是 VDDQ/2，
所以高準位靠近 VDDQ、低準位由「驅動強度與 ODT 的分壓」決定，兩者不對稱。

Micron 的點對點設計指南舉的例子（`TN_4040`）：

```
VDDQ = 1.2 V、控制器 RON = 34 Ω、顆粒 ODT = 60 Ω
低準位 = 1.2 × 34/(34+60) = 434 mV
理想 Vref = 434 + (1200 − 434)/2 = 816 mV = 0.68 × VDDQ
```

**這意味著改變 ODT 或驅動強度會改變最佳 Vref。** DDR4 因此提供 VREFDQ
訓練；DDR5 更進一步做到 per-DRAM 校準。

## 3.2 終端（ODT, On-Die Termination）

把終端電阻做在晶片裡，而不是板子上。理由是省面積，也讓「只有正在通訊的
那一端開終端」成為可能。

**ODT 值的取捨**：
- 阻值**低**（如 40 Ω）：反射壓得好，但**電流大**、低準位被拉高、眼高變小
- 阻值**高**（如 120 Ω）：眼高大、省電，但反射抑制差、ISI 變大

實務上寫入時顆粒開 ODT（如 60 Ω）、讀取時控制器開 ODT，
**沒在通訊的那一端關掉**。

## 3.3 驅動強度（Drive Strength / RON）

驅動器的輸出阻抗。DDR4 規格是 **34 Ω 與 48 Ω**（Micron 自家用 40 Ω）。

**核心原則：驅動器阻抗要匹配傳輸線阻抗。** 匹配的話，從遠端反射回來的
能量會被驅動器吸收，不會再彈回去。不匹配就會在線上來回震盪，
邊緣變髒、眼睛變小。

Micron 的建議組合表（`TN_4040` 第 27 頁）給的判定門檻是：

> Aperture DC ≥ 70%、電壓裕度 ≥ 100 mV、過衝 ≤ 200 mV

## 3.4 這一節與模擬的關係

**驅動強度與 ODT 是設計選擇，不是量測結果。** 它們寫在記憶體控制器的
暫存器裡。模擬工具沒有辦法從板子推出來，**必須由你指定**。

指定錯了不會報錯——眼圖照樣畫得出來，只是那張圖對應的是另一組沒人會用的
組態。這是 DDR 模擬最常見的靜默錯誤之一。

---

# 四、時序：裕度是怎麼算出來的

## 4.1 UI 是一切的尺

```
UI（Unit Interval）= 1 / 資料率
DDR3L-1866  →  1/1866 MHz = 536 ps
DDR4-3200   →  1/3200 MHz = 312 ps
DDR5-6400   →  1/6400 MHz = 156 ps
```

**任何單獨一項超過一個 UI 的時序數字都是算錯的。** 一個位元的眼睛不可能
比一個位元還寬。這是最快的自檢。

## 4.2 setup 與 hold

```
        ←─ tDS ─→ ｜ ←─ tDH ─→
資料 ____╱‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾____
                  ↑
             選通邊緣（取樣時刻）
```

- **tDS（Setup）**：資料必須在選通邊緣**之前**站穩多久
- **tDH（Hold）**：資料必須在選通邊緣**之後**維持多久

這兩個是**規格向你要求的**，不是你量到的。

## 4.3 裕度 ＝ 量到的時間 − 規格要求

**這是整份文件最重要的一句話。**

```
量到的 Setup 時間   197.5 ps
規格要求 tDS       −140.0 ps
──────────────────────────
Setup 裕度           57.5 ps
```

模擬工具量到的是「資料實際提前了多久」，那不是裕度。很多人把量到的
197.5 ps 當成裕度，於是以為有三倍餘裕，實際只有 57.5 ps。

## 4.4 AC 與 DC 兩組門檻

規格會給四個電壓門檻：

```
VIH(AC) / VIL(AC)    要「衝到」的準位，比較嚴
VIH(DC) / VIL(DC)    要「維持」的準位，比較鬆
```

進門要跨過門檻（AC），進門之後待在屋裡就好（DC）。

**兩組填成一樣會讓 Hold 系統性偏小**——你要求訊號維持在較嚴的準位上，
它會比較早被判定為「離開」。

## 4.5 Derating（降額）

JEDEC 給的 tDS／tDH base 值假設訊號邊緣有一定的斜率。**你的邊緣比較慢，
要求就要變嚴**——因為慢邊緣通過門檻區的時間長，不確定性大。

所以實際要求是：

```
tDS(實際) = tDS(base) + ΔtDS(依你的 slew rate 查表)
```

這張表在 JEDEC 規格書裡，**工具不會替你填**。

## 4.6 tVAC

Valid AC time——訊號在 AC 準位**之外**待了多久。

為什麼要單獨管：訊號「碰一下門檻就回來」與「衝過去待著」，在某些量法下
看起來一樣，但只有後者接收端才真的讀得到。

## 4.7 DDR4 換了一整套：遮罩 ＋ BER

**DDR4 之後，接收端要求改用 Rx Mask（遮罩）＋ BER 定義，取代 setup／hold
與 DC 電壓擺幅。**（DesignCon `Accurate_Statistical_Based_DDR4_Margin
_Estimation…` 第 5 頁）

| | DDR3 | DDR4 以後 |
|---|---|---|
| 判準 | tDS／tDH ＋ 準位 | 眼圖要容納一個**遮罩**，BER < 10⁻¹⁶ |
| Slew rate derating | **需要** | **不需要**（遮罩已內含） |
| 抖動 | 另外處理 | 遮罩內含確定性 ＋ 隨機抖動 |

DDR4 的 DQ 遮罩是 **眼寬 0.2 UI、眼高 130 mV**。

好處是簡潔：一個遮罩就把時間與電壓的要求說完，也不必再查 derating 表。
代價是**你必須做得出統計眼圖**（要能外推到 10⁻¹⁶），暫態跑幾百個 UI 是
不夠的。

---

# 五、位址命令群：另一種題型，不是資料群的變體

前面四章講的都是資料群。位址命令群（ADCM／CA）看起來只是「另外幾條線」，
但它**六件事全部不一樣**：時間基準、UI、拓撲、埠數、規格參數、參考電壓。

把資料群的做法搬過來，眼圖照樣畫得出來，數字照樣出得來，**而且看起來很正常**。

| | 資料群 | 位址命令群 |
|---|---|---|
| 時間基準 | DQS 差動交越 | **CK 差動交越** |
| 速率 | DDR（兩緣） | **SDR（只有上升緣）** |
| 一個位元佔多久 | 1 UI | **1 或 2 個時脈週期**（見 5.2） |
| 方向 | 雙向（讀／寫） | **單向**（只有控制器驅動） |
| 拓撲 | 點對點 | **Fly-by 一對多 ＋ 遠端終端** |
| 一條網路幾個埠 | 2 | **顆粒數 ＋ 2** |
| 規格參數 | tDS／tDH | **tIS／tIH** |
| DDR4 判準 | 遮罩 ＋ BER | **仍然是 setup／hold ＋ derating** |
| I/O 型態（DDR4） | POD | **SSTL 推挽** |
| Vref（DDR4） | VREFDQ，內部訓練，≈0.68×VDDQ | **VREFCA，外部供給，0.5×VDD** |

最後三列是 DDR4 才出現的不對稱，特別容易踩到——**同一片板子上兩群要用兩套
判準、兩個 Vref**。

---

## 5.1 時間基準是 CK

位址與命令在 **CK_t／CK_c 穿越零** 的時刻被取樣。與資料群一樣，
**不可以用單腳穿越 Vref 代替**——共模會隨串擾與 SSN 浮動。

差別在於：DQS 是「跟著這一筆資料一起送出來」的選通，CK 是**整條匯流排共用**
的時脈。所以 CK 的品質是全域問題：它壞了，位址、命令、以及每一顆顆粒的
內部時序全部跟著壞。

CK 本身要量的東西也比 DQS 多：週期抖動（`tJIT(per)`）、
累積抖動（`tJIT(cc)`）、工作週期（`tCH`／`tCL`）。這些在資料群不必單獨看，
因為 DQS 只要相對 DQ 對齊就好。

---

## 5.2 1T、2T、½T——這個選擇直接改變裕度的分母

**這是位址群獨有的、而且最常被漏掉的一個設定。**

「命令速率（Command Rate）」決定一個位址值在匯流排上**維持幾個時脈週期**：

```
1T   位址每個時脈週期換一次；CS_n 每個週期都可以下命令
     ┌──A──┬──B──┬──C──┐        位址視窗 ＝ 1 × tCK
     └─CS──┴─CS──┴─CS──┘

2T   位址維持兩個時脈週期；CS_n 只在**第二個**週期拉低
     ┌─────A─────┬─────B─────┐  位址視窗 ＝ 2 × tCK
     └──··──┴─CS─┴──··──┴─CS─┘

½T   CA 匯流排在時脈兩緣都取樣（LPDDR4／5 的 CA 就是這樣）
     位址視窗 ＝ 0.5 × tCK
```

### 為什麼會有 2T

位址是**一對多**：一個驅動器要推 8 到 16 顆顆粒的位址腳。負載重、扇出大，
邊緣就慢；邊緣一慢，JEDEC 的 derating 表就把 tIS／tIH 的要求變嚴
（見 4.5）。兩頭夾攻，1T 的視窗常常不夠。

**2T 用一個時脈週期的延遲，換來加倍的位址視窗。** 代價是每個命令都晚一拍，
效能掉幾個百分點；換來的是位址匯流排能收斂。

注意 **CS_n 不跟著變成 2T**——它仍然只拉低一個週期，否則同一個位址會被
解讀成兩個命令。所以 2T 模式下，位址線與 CS_n **要用不同的視窗去判**。

### 對模擬的意義

```
tCK（時脈週期） = 2 / 資料率
DDR3L-1866：資料率 1866 MT/s → tCK = 1.071 ns
    資料群 UI  =  535.5 ps
    位址 1T    = 1071   ps      ← 資料群的兩倍
    位址 2T    = 2143   ps      ← 資料群的四倍
```

**裕度 ＝ 量到的時間 − 規格要求**（4.3），而「量到的時間」的搜尋範圍要用
位址自己的視窗，不是資料群的 UI。用錯的後果：

* 拿 DQ 的 UI（535 ps）去量 2T 的位址（2143 ps）→ 搜尋範圍只有實際的四分之一，
  大量位址線會回報「這個位元在範圍內沒有轉態」，看起來像量不到。
* 反過來，把 2T 當成 1T → 視窗少一半，裕度系統性偏小，會去改一個沒有問題的板子。

**開始分析前先問清楚控制器設的是 1T 還是 2T。** 這是暫存器設定，
從板子上量不出來，也推不出來。

---

## 5.3 拓撲：Fly-by 讓「一條道兩個埠」的假設不成立

資料群是點對點：控制器一個埠、顆粒一個埠，一條道兩個埠。

位址群是 **fly-by**：

```
控制器 ──┬──────┬──────┬──────┬──── Rtt ── VTT
         │      │      │      │
        U1     U2     U3     U4        （四顆顆粒，各一個短分支）
```

於是**一條位址網路的埠數是「顆粒數 ＋ 2」**（控制器 1 ＋ 每顆 1 ＋ 終端 1）。
16 條位址線配 4 顆顆粒就是 96 個埠。

這帶來三個實務後果：

**一、每一顆都要量。** 各顆粒看到的波形不同——離終端近的比較乾淨，
中間的分支點會有反射。**最差的不一定是最遠那顆**，這一點跟直覺相反，
所以要逐顆量再取最差，不要只量末端。

**二、每個分支都是殘段（stub）。** 顆粒到主幹的那一小段沒有終端，
是開路殘段——那正是 SIwave 最不擅長的結構（λ/4 共振被放大）。
位址群比資料群更需要確認求解器的可信頻寬。

**三、VTT 終端電阻是電路的一部分。** 它不是「未接的埠」，
不能當成開路處置掉。忘了它，末端的反射會完全不同。

### Write leveling 是設計，不是誤差

Fly-by 讓每顆顆粒收到 CK 的時刻都不同（那正是 fly-by 的目的：分散 SSN）。
DDR3 之後用 **write leveling** 讓控制器替每顆顆粒各自延遲 DQS，補掉這個差。

所以看到「各顆粒的 CK 到達時間差了幾百 ps」時，**那不是佈線錯誤**。
但位址對 CK 的相對關係**在每一顆的位置上都必須成立**——因為位址沒有
per-device 的訓練可以補。這就是為什麼要逐顆量。

---

## 5.4 規格參數與電壓

### tIS／tIH，不是 tDS／tDH

位址與命令用 `tIS`（input setup）與 `tIH`（input hold），相對 **CK** 量。
與資料群一樣要查 derating 表——而且位址的邊緣通常更慢（負載重），
derating 的懲罰更明顯。

CKE 另有自己的參數（`tIHCKE` 等），因為它會影響進出低功耗狀態的時序。

### DDR4 的不對稱

第四章講過 DDR4 的資料群改用 **Rx 遮罩 ＋ BER**，不再用 setup／hold ＋ derating。

**位址命令群沒有跟著改，仍然是 tIS／tIH ＋ derating。**

所以做一片 DDR4 板子時，兩群要用**兩套完全不同的判準**：
資料群比遮罩、位址群算裕度。用同一套跑完兩群，其中一群一定是錯的。

### VREFCA ≠ VREFDQ

| | DDR4 資料群 | DDR4 位址命令群 |
|---|---|---|
| I/O 型態 | POD（偽開汲極） | **SSTL（推挽）** |
| 終端接到 | VDDQ | **VTT ＝ 0.5×VDD** |
| Vref | VREFDQ，**內部訓練**，典型 ≈0.68×VDDQ | **VREFCA，外部供給，0.5×VDD** |

**拿 0.68×VDDQ 去判位址就是錯的**，而且錯得不明顯——只是所有位址的
setup 系統性偏大、hold 系統性偏小。

---

## 5.5 邊緣單調性：位址群特有的失效模式

資料群通常只看眼寬眼高。位址群還要看一件事：**邊緣在 VIL(AC) 到 VIH(AC)
之間有沒有轉平或回頭**。

Fly-by 的分支反射會在邊緣中段疊出一個台階：

```
  VIH(AC) ─────────────────╱───
                        ╱
  Vref    ────────────┤        ← 台階：訊號在這裡停住甚至回頭
                    ╱
  VIL(AC) ──────╱───────────────
```

JEDEC 要求訊號在這個區間內**單調**。不單調的話接收端可能在同一個邊緣上
判讀兩次（double clocking）——而**眼圖看不出來**：折疊起來之後那個台階
被其他位元蓋掉了。

**要用單一波形（single-shot）看，不是眼圖。** 這是位址群一定要另外做的檢查。

---

## 5.6 位址群的自檢清單

* [ ] 時間基準用的是 **CK 差動交越**，不是 DQS、不是單腳穿越 Vref
* [ ] 已經確認控制器設的是 **1T 還是 2T**（或 LPDDR 的 ½T）
* [ ] 搜尋視窗用的是**位址自己的**週期，不是資料群的 UI
* [ ] CS_n 用 1T 的視窗判，即使位址是 2T
* [ ] 埠數 ＝ 顆粒數 ＋ 2（含 VTT 終端），不是 2
* [ ] **每一顆都量過**，不是只量最遠那顆
* [ ] VTT 終端電阻在電路裡，沒有被當成未接的埠
* [ ] 用的是 `tIS`／`tIH` 加 derating，不是 `tDS`／`tDH`
* [ ] DDR4：位址仍算裕度，**沒有**跟著資料群改用遮罩
* [ ] DDR4：位址用 **VREFCA ＝ 0.5×VDD**，不是資料群那個 0.68×VDDQ
* [ ] 另外用單一波形檢查過邊緣單調性（眼圖看不出來）
* [ ] 求解器的可信頻寬有考慮到分支殘段（比資料群更嚴）

---

# 六、模擬流程的六個階段

```
① Layout（.brd/.aedb）
      ↓ 選網路、裁切
② 通道幾何（只剩要分析的那一組）
      ↓ 建 Port
③ 有埠的模型
      ↓ 電磁場求解（SIwave 或 HFSS）
④ S 參數（Touchstone，N 埠）
      ↓ 接上 IBIS 緩衝器
⑤ 電路模擬（Transient 或統計）
      ↓ 以選通為基準量測
⑥ 眼圖 ＋ 逐訊號裕度 ＋ Pass/Fail
```

每一階段的產出都可以獨立檢查，**這很重要**——出錯時你要知道是哪一段錯。

## 6.1 ① → ② 裁切

從整片板子切出只含這一組訊號的區域。目的是把求解規模降下來。

**要保留的**：訊號線本身、**參考平面**、**鄰近的接地過孔**、元件的接腳。

**最常見的錯**：外框貼太緊，把 BGA 逃線區周圍的接地球切掉了。
訊號還在，但回流路徑沒了，之後建 Port 的參考端會站不住。

## 6.2 ② → ③ 建 Port

在通道的兩端（控制器與顆粒的接腳）建立激勵埠。

**Port 的正端接訊號腳，負端接參考。** 負端怎麼定義是關鍵：

| 方式 | 負端 | 適用 |
|---|---|---|
| Coax（同軸） | 過孔周圍的參考平面 | 通孔結構 |
| Circuit（座標） | 參考平面上的某個點 | 一般 SMD |
| **Pin Group** | **該元件所有參考腳合起來** | **細間距 BGA** |

**細間距 BGA 一定要用 Pin Group。** 因為訊號球正下方本來就是 antipad
（平面挖空），座標型的負端在那裡站不住。求解器遇到接不好的埠常常是
**靜默丟掉**——不報錯、照樣解完，只是輸出的 Touchstone 少了幾個埠。

## 6.3 ③ → ④ 電磁場求解

**SIwave（混合式）vs HFSS（全波）怎麼選？**

不是「複雜就用 HFSS」，而是**看你要用到幾 GHz**。三維複雜度是相對於頻率
才有意義的：同一顆 BGA launch，在 20 GHz 是必須用 HFSS 的結構，
在 3 GHz 只是一小段集總電感。

實測的參考值（不同工具與版本會有差異，自己驗一次比較保險）：

| 結構 | SIwave 可信到 |
|---|---|
| 均勻帶狀線、平面走線 | 很高，本來就是它的強項 |
| 走線彎折 | 不是問題（實測與 HFSS 差 0.3 dB） |
| 跨越參考平面分割 | 約 13 GHz |
| **開路殘段／T 型分支** | **約 7 GHz**（λ/4 共振處被放大） |

DDR3L-1866 的邊緣拐點約 2～3.5 GHz、DDR4-3200 約 5 GHz，
**都在 SIwave 的可信範圍內**。所以 DDR 大多數情況用 SIwave 就夠，
而且快一到兩個數量級。

**掃頻上限怎麼定**：至少到 5 倍基頻，或用 `f_knee = 0.35 / Tr` 推。
DDR3L-1866 基頻 933 MHz，掃到 5～10 GHz 綽綽有餘。掃到 50 GHz 只是浪費。

## 6.4 ④ → ⑤ 接 IBIS

把 S 參數當成中間的通道，兩端接上緩衝器模型。

**DDR 的兩端永遠是兩家料號**：控制器一份 `.ibs`、顆粒一份 `.ibs`。
只給一份會讓其中一側用錯驅動器——而且**不會報錯**。

**一次要把整組道一起送進去求解**，不能拆成一條一條算。拆了就丟失道間耦合，
串擾整個不見。

## 6.5 ⑤ → ⑥ 量測

以**選通的差動交越點**為時間原點，逐條資料訊號量 setup／hold。

不能用單腳穿越 Vref 代替——DQS 的共模會隨串擾浮動，差動相減才把它消掉。

---

# 七、每一階段真正的決定點

這一節列出**會改變答案**的選項。其餘都是預設值就好。

## 7.1 參考網路要選哪些

DDR 的回流**同時走地與 VDDQ**（POD 的終端接到 VDDQ）。只給 GND
會少一半的回流路徑。

## 7.2 選通相對資料的相位

**寫入**：控制器把 DQS 對齊在資料眼的**正中央**（相對資料位移 0.5 UI）。
**讀取**：顆粒送出的 DQS 是**邊緣對齊**，由控制器自己延遲 90° 再取樣。

**不做這個位移的話，量到的 setup 與 hold 都會趨近於零**——
看起來像通道爛掉，其實只是沒對齊。

## 7.3 碼型（Pattern）

| 碼型 | 用途 | 為什麼不能互換 |
|---|---|---|
| **決定性最差** | 看串擾 | 刻意讓受害線長時間持平好讓攻擊者對齊 |
| **PRBS／隨機** | 量時序 | 有頻繁轉態，才有 setup／hold 可量 |

用最差碼型量時序，常常**一個約束都量不到**（因為選通邊落在沒有轉態的
地方）。用隨機碼型看串擾，則**幾乎確定沒看到最差**——N 條道同時反向切換的
機率是 (1/4)^(N−1)，8 條道要跑兩萬個 UI 才有一次期望值。

## 7.4 未接的埠要怎麼處置

多埠檔裡沒被綁到模型的埠，必須明確指定**終結、開路還是接地**。
開路與終結會給出不同的串擾結果，**兩者都不是安全預設**。

## 7.5 讀還是寫

雙向匯流排的兩個方向都真實存在，而且是兩張不同的眼圖。工具沒有依據代選。

注意 **DM（資料遮罩）在顆粒端是單向的**——讀取時顆粒沒有 DM 的驅動器。
這不是錯誤，是 DDR 本來就這樣；做讀取分析時要把 DM 排除。

---

# 八、進階：串擾、SSN、PDN、ODT 非線性

前面六章做完，你會得到一張「乾淨」的眼圖。實機量到的通常比它差，
差在這一章。

## 8.1 串擾

DDR 的串擾有兩個特性 SerDes 沒有：

**一、攻擊者是七條，不是一條。** 一個 byte lane 裡受害線的鄰居全部會動。

**二、最惡劣的對齊必然發生。** 因為資料是任意的，八條線遲早會出現
「其餘七條同時往反方向切換」那一拍。所以不能用機率論證「這種情況很少」。

**遠端串擾（FEXT）與近端串擾（NEXT）**：DDR 短距離、雙向，兩者都要看。
FEXT 隨長度累積，NEXT 在驅動端最大。

## 8.2 SSN（Simultaneous Switching Noise，同時切換雜訊）

多條 I/O 同時切換時，電流從電源網路抽取，在封裝與 PCB 的電感上產生壓降，
**讓晶片內部的「地」跟著跳**。後果：

- 輸出的高低準位跟著偏移 → 眼高變小
- 切換時刻跟著偏移 → **抖動（PSIJ, Power Supply Induced Jitter）**

實測數量級（DDR5，Xpeedic 2022）：加入實際 PDN 之後
**眼寬 −13 ps、眼高 −8 mV**。

DDR3/DDR4 的 SSN 深入分析（DesignCon, Yoon）測到的抖動：

| 條件 | 抖動 |
|---|---|
| 理想（無 ISI、無串擾、無 SSN） | 0.058 UI |
| ＋ISI | 0.078 UI |
| ＋ISI ＋串擾 | 0.183 UI |
| ＋ISI ＋SSN | 0.216 UI |
| **＋ISI ＋串擾 ＋SSN** | **0.31 UI** |

**單看通道的眼圖會嚴重高估裕度。** 0.058 → 0.31 是五倍。

## 8.3 PDN 要不要一起模

理想上要。做法是把 PDN 的 S 參數與通道一起接進電路模擬，讓緩衝器的
電源腳看到真實的阻抗，而不是理想電壓源。

代價是模型變大、求解變慢。折衷做法（DesignCon 的 MCF 方法）：

1. 跑兩次暫態：一次接理想電源、一次接真實 PDN
2. 取兩者的眼高／眼寬差 → **MCF（Mask Correction Factor）**
3. 把 MCF 加到遮罩上，之後的統計分析就用修正過的遮罩

實測結果：DDR4 遮罩從 眼寬 0.2 UI／眼高 130 mV 修正成
**0.23 UI／224 mV**，而模擬與實測的差異只有 2%。

## 8.4 ODT 的非線性（很少人注意）

ODT 有兩種做法：

| | 被動式（Poly 電阻） | 主動式（PMOS） |
|---|---|---|
| 面積 | 大 | **小** |
| PVT 敏感度 | 低 | **高** |
| I-V 線性度 | 好 | **差** |

現代 DDR 用主動式，所以**終端阻抗其實不是常數**——它隨輸出電壓變。

DesignCon（Yoon）的實測結論很反直覺：

> **校準點（Calibration Point）比線性度更重要。** POD 拓撲下，
> 校準在 0.75×VCC 比 0.35×VCC 的抖動更好（0.254 vs 0.31 UI），
> 即使前者的串擾更大。

意思是：IBIS 模型裡那條 I-V 曲線是在某個工作點校準的，
**你的實際工作點跟它不同的話，等效阻抗就不是標稱值**。

---

# 九、DDR5 與 IBIS-AMI

## 9.1 為什麼 DDR5 要用 AMI

3200 → 6400 MT/s，通道損耗已經大到需要**等化**：
FIR（發送端預加重）、CTLE、DFE。這些是行為模型，普通 IBIS 描述不了。

## 9.2 普通 IBIS-AMI 用在 DDR5 的四個限制

Xpeedic 在 2022 Asian IBIS Summit 列的（`DDR5.pdf` 第 4 頁）：

1. **不支援 PDN 效應**
2. **只支援差動訊號**——但 DDR 的 DQ 是單端
3. **只支援點對點**——DDR 有多 Rank、多 DIMM
4. **不支援外部時脈**——但 DQ 是被 DQS 觸發的

## 9.3 三個 DDR5 特有的技術點

**DER（bit-by-bit）取代 SBR。** 單端訊號的上升與下降邊緣**不對稱**
（POD 的高低準位不對稱造成），SerDes 的差動訊號沒這問題，所以只需要一個
步階響應。DDR 要**分別取上升與下降的步階響應**才算得準。

**DC_Offset（BIRD197.7）。** 單端訊號不是繞著零擺動，統計眼圖要先把
直流偏移拿掉再算，最後再加回去：

```
V_dc_offset = (V_high + V_low) / 2
```

**時脈前饋（Clock Forwarding）。** IBIS 7.1 開始，Rx 的 `AMI_GetWave`
可以同時吃資料與時脈波形——因為 DDR 的取樣時刻來自 DQS，
不是從資料裡回復。

---

# 十、Corner 與統計

## 10.1 要跑哪些 Corner

IBIS 模型提供三組：`typ`／`min`／`max`。**注意命名**：

```
min = Slow（慢邊緣、弱驅動、低電壓、高溫）
max = Fast（快邊緣、強驅動、高電壓、低溫）
```

## 10.2 最差不一定是 Slow

直覺會說「慢的最差」，但**快邊緣的反射更大**——上升時間短，
阻抗不連續造成的反射能量高，串擾也大（串擾正比於 dV/dt）。

所以**三個都要跑，最差由結果排序決定**。我在實際案子上就遇過
Fast 是最差的那一個。

## 10.3 暫態 vs 統計

| | 暫態（Transient） | 統計（Statistical） |
|---|---|---|
| 做法 | 真的算幾百個 UI 的波形 | 由脈衝響應摺積出機率分布 |
| 能到的 BER | 10⁻³ 左右 | 可外推到 10⁻¹⁶ |
| 能不能看串擾最差 | **可以**（用決定性碼型） | 只能統計 |
| 非線性 | **保留** | 假設線性 |

DDR4 之後的遮罩判定要求 BER 10⁻¹⁶，**必須用統計方法**；
但串擾的最惡劣對齊要用暫態的決定性碼型。兩種都要跑。

---

# 十一、怎麼知道自己算錯了

按順序檢查，每一條都是實際踩過的。

## 11.1 幾何與埠

- [ ] 裁切後**接地過孔還在**嗎？（BGA 周圍最容易被切掉）
- [ ] Touchstone 的**埠數對嗎**？22 個埠就該是 `.s22p`。
      求解器丟埠是**靜默的**，副檔名是唯一的偵測方式。
- [ ] 每個埠都有**有效的參考**嗎？沒有的話會全反射。

## 11.2 S 參數本身

- [ ] 插入損耗在基頻是**零點幾 dB**的量級嗎？如果是 −50 dB，
      你多半量到了串擾而不是直通路徑。
- [ ] 回波損耗有沒有比 −10 dB 還糟的頻段？
- [ ] **被動性**：|S| 有沒有大於 1？
- [ ] **因果性**：階躍響應在 t=0 之前有沒有東西？

## 11.3 時序數字

- [ ] setup 或 hold **單獨一項有沒有超過一個 UI**？超過就是錯的。
- [ ] setup ＋ hold 加起來合理嗎？應該接近「UI 減去邊緣時間」。
- [ ] 兩者都**趨近於零**？那是選通沒有做相位對齊。
- [ ] 大部分道**量不到約束**？那是碼型選錯了（用了最差碼型量時序）。

## 11.4 模型綁定

- [ ] 兩端綁到的是**不同料號**嗎？DDR 不可能兩端同型號。
- [ ] 驅動強度與 ODT 是**你指定的值**，還是工具猜的？
- [ ] 讀取方向有沒有把 **DM 排除**？

## 11.5 結論

- [ ] **裕度**是「量到的時間 − 規格要求」，不是量到的時間本身？
- [ ] tDS／tDH 有沒有含 **derating**？
- [ ] 三個 Corner 都跑了嗎？最差的是哪一個？
- [ ] 有沒有把「PRBS 跑出來的結果」標成最差情況？（不可以）

---

# 附錄：延伸閱讀

以下 19 份 DDR4／DDR5 延伸教材，建議依序閱讀：

| 順序 | 檔案 | 為什麼 |
|---|---|---|
| 1 | `TN_4040_DDR4_Point_to_Point_Design_Guide.pdf` | Micron 官方，最實用。VREF 計算、POD、驅動與 ODT 建議組合表 |
| 2 | `6_TH5Slides_DDR4BoardDesignAndSignal.pdf` | 板級設計全貌 |
| 3 | `Accurate_Statistical_Based_DDR4_Margin_Estimation…` | DDR4 為何改用遮罩＋BER，以及 MCF 方法 |
| 4 | `SLIDES_07_IndepthSSNAnalysisofDDR3DDR4_Yoon.pdf` | ODT 非線性與 SSN 對抖動的實測 |
| 5 | `DDR5.pdf` | DDR5 的 AMI 需求：DER、DC_offset、時脈前饋 |
| 6 | `6-TA2_Slides_ChanneltoChannelCrosstalkBehavior.pdf` | 串擾行為 |
| 7 | `11-TA2_Slides_InnovativePDNDesignGuidelines.pdf` | PDN 設計 |
| 8 | `11_WE5Slides_Touchstonev2SIPISParameterModels.pdf` | Touchstone v2 與 S 參數模型品質 |

其餘為 LPDDR3／LPDDR4 專題與各家實作經驗，做到那些世代再看。

---

## 一句話總結

**DDR 模擬的難處不在算得準，而在「算的是不是你以為的那件事」**——
方向對不對、時間基準是不是選通、裕度有沒有減掉規格要求、
最差碼型有沒有真的出現過。這些每一項出錯，眼圖都畫得出來，
而且看起來都很正常。
